spring:
  main:
    allow-bean-definition-overriding: true

  rabbitmq:
    host: ${RABBIT_URL}
    port: 5672
    virtual-host: /
    username: ${RABBIT_USER}
    password: ${RABBIT_PASSWORD}

  cloud:
    config:
      enabled: false

  jpa:
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

    hibernate:
      ddl-auto: update

  application:
    name: meta-store

  datasource:
    # CHANGEME
    url: jdbc:postgres://zdgmiriwayiuuk:8d90c3ed62757d9b36fceb9c1460dffdb51b8d9322d90e50a3e4e58943bf9edd@ec2-52-18-116-67.eu-west-1.compute.amazonaws.com:5432/dea9rorslm9bbv
    username: zdgmiriwayiuuk
    password: 8d90c3ed62757d9b36fceb9c1460dffdb51b8d9322d90e50a3e4e58943bf9edd

security:
  # CHANGEME
  configurations:
    - path: /somepath
      role: somerole

keycloak:
  auth-server-url: ${AUTH_URL}
  realm: ${AUTH_REALM}
  resource: ${AUTH_RESOURCE}
  public-client: true

server:
  address: 0.0.0.0
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: ${REGISTRY_URL}
    registerWithEureka: true
    fetchRegistry: true

management:
  port: 8081
  endpoints:
    web:
      exposure:
        include: '*'